import { describe, it, expect } from "vitest";

describe("The docker volume function", () => {
  it("should list all existing volumes", async () => {
    // console.log("dockerClient");
    expect(1).toBe(1);
  });
});
